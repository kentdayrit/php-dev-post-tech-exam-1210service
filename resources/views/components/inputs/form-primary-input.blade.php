<div class="input-group mb-3">
    <span class="input-group-text">{{ $title }}</span>
    <input type="{{ $type }}" class="form-control" placeholder="{{ $placeholder }}" value="{{ $value }}" id="{{ $id }}" name="{{ $name }}">
</div>